// Dependencies for the Node.js backend (example)
{
  "name": "trustlance-backend",
  "version": "1.0.0",
  "description": "Backend API for TrustLance monorepo",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "dev": "nodemon src/index.ts",
    "prisma:seed": "ts-node ./prisma/seed.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "@prisma/client": "^5.15.0"
  }
}