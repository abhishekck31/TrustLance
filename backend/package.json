{
  "name": "trustlance-backend",
  "version": "1.0.0",
  "description": "Backend for TrustLance Web3 Monorepo",
  "main": "src/server.ts",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "seed": "ts-node src/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "prisma": "^5.14.0",
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "nodemon": "^3.1.0"
  }
}